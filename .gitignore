# Reference clones of the existing OOD apps, kept locally for convention
# lookup. They are separate git repositories and must never be committed.
.artie/reference_repos/

# Test image cache: multi-hundred-MB Apptainer sandboxes and SIFs.
tests/.cache/

# Subagent-driven-development scratch workspace.
.superpowers/

.claude/.cc-writes

# Stray installer downloads. Miniforge3-Linux-aarch64.sh is ~120MB; committing
# it would put it in history permanently. Ignored rather than deleted - they are
# not ours to remove.
/Miniforge3-*.sh
/micromamba.sh
