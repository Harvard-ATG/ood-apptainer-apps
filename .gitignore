# Local working directory: design docs, plans, course requests, preflight
# artifacts, and reference clones of the sibling OOD apps. Deliberately not
# tracked -- these are working artifacts, and the reference clones are separate
# git repositories besides.
.artie/

# Test image cache: multi-hundred-MB Apptainer sandboxes and SIFs.
tests/.cache/

# Subagent-driven-development scratch workspace.
.superpowers/

.claude/.cc-writes

# Local image build output. Artifacts are immutable and belong on the
# cluster image roots, never in git.
/build/
